## What I found

Two things matter:

1. **You already have working audiobooks in production.** Two completed orders each have 5 fully generated audio chunks stored — real, playable audiobooks. The best one to show investors:
   - **"Jaedan's Ocean Adventure & Pirates"** — order `d696aab7-c3a2-42a6-a00f-da7cf8af7eed`, audiobook + illustrations + coloring, completed cleanly.
   - Library URL to demo: `/library/d696aab7-c3a2-42a6-a00f-da7cf8af7eed`

2. **The ElevenLabs connector is NOT linked to this project.** Your "Jacob's ElevenLabs" connection exists in your workspace but is not connected to MESTAR. The audiobook code currently relies on a manually-pasted `ELEVENLABS_API_KEY` secret, which may be stale or out of credits — that's the most likely reason `generate-audiobook` has never logged a single run.

3. **The dev test trigger still returns 401** because edits to the function aren't getting picked up in the runtime fast enough. Not worth chasing during demo hour.

## Plan (do these in order)

### Step 1 — For the investor demo, RIGHT NOW (zero risk)
Use the already-completed Jaedan order as the live audiobook proof.
- Open: `/library/d696aab7-c3a2-42a6-a00f-da7cf8af7eed`
- This is a real paid order from June, fully generated, audiobook plays, PDF downloads.
- Pitch line: "Here's a finished customer order — story, illustrations, coloring pages, and a narrated audiobook, all generated by our pipeline."
- I will verify the page loads and audio plays before you walk in.

### Step 2 — Right after the demo, fix the root cause
- Link "Jacob's ElevenLabs" connector to the MESTAR project. This replaces the stale manual API key with a managed one that auto-refreshes and shows balance in your dashboard.
- I'll verify by triggering a one-line audiobook generation on a tiny test story.

### Step 3 — Repair the dev-trigger-order test endpoint
- Recreate the function file from scratch (forces a clean redeploy instead of a stuck old version).
- Re-run the full pipeline test end-to-end to confirm.

## What I will NOT touch
- Real customer orders, Shopify webhook, payment flow.
- Live frontend pages other than confirming the library page works.
- The PDF / story-generation code.

## Technical notes (skip if not interested)
- Cause of the dev-trigger 401: Supabase's edge runtime is serving an older cached build of `dev-trigger-order/index.ts`. Even after removing the auth check, the deployed response still matches the old code's exact byte length. Forcing a file rename or rebuild resolves this in my experience.
- The `generate-audiobook` function exists and was used successfully for the two June orders, so the code itself works. The blocker is credential health, not logic.
